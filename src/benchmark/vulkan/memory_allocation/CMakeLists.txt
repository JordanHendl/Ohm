if(Build_Benchmarks)
find_package(benchmark)
if(benchmark_FOUND)
  add_executable(benchmark_ohm_vulkan_allocation benchmark.cpp)
  target_link_libraries(benchmark_ohm_vulkan_allocation benchmark::benchmark vulkan)
endif()
endif()
